{
  "name": "gpt-image-2-jp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run parse && astro build",
    "preview": "astro preview",
    "fetch-sources": "node scripts/fetch-sources.mjs",
    "parse": "node scripts/parse-all.mjs",
    "update": "npm run fetch-sources && npm run parse"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
